spring:
  security:
    oauth2:
        client:
          registration:
            github:
              client-id: { your client id here }
              client-secret: {your client secret here}
            google:
              client-id: { your client id here }
              client-secret: { your client id here }